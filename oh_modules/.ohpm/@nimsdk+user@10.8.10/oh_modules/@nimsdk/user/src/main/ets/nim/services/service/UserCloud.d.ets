import { NIM, V2NIMUserUpdateParams } from '@nimsdk/base'; import { V2NIMUserImpl } from '../model/V2NIMUserImpl'; export declare class UserCloud { private core; constructor(o4: NIM); getUserList(j4: string[]): Promise<V2NIMUserImpl[]>; updateSelfUserProfile(g4: V2NIMUserUpdateParams): Promise<number>; addUserToBlockList(accountId: string): Promise<void>; removeUserFromBlockList(accountId: string): Promise<void>; } 