import Logger from "@nimsdk/base/src/main/ets/nim/utils/logger"; export declare class TeamNotificationProcessor { private queue; private processing; private logger; constructor(c18: Logger); enqueueEvent(event: TeamNotificationEvent): void; private processQueue; } export declare class TeamNotificationEvent { private _eventName; private _cmd; private _handler; constructor(eventName: string, cmd: string, handler: () => Promise<void>); eventName(): string; handler(z17: Logger): Promise<void>; } 