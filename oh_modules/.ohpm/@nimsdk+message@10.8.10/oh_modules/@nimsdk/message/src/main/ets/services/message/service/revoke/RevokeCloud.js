import { V2NIMConversationType } from '@nimsdk/base'; import { formatRevokeNotification, formatSendRevokeMessage } from '../../utils/Format'; export class RevokeCloud { constructor(u65) { this.core = u65; } async revokeMessage(message, params) { const cmd = message.conversationType === V2NIMConversationType.V2NIM_CONVERSATION_TYPE_SUPER_TEAM ? 'v2RevokeSuperTeamMessage' : 'v2RevokeMessage'; await this.core.sendCmd(cmd, { 'tag': formatSendRevokeMessage(this.core, message, params) }); return formatRevokeNotification(this.core, message, params); } } 