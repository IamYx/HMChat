import { NIM } from '@nimsdk/base'; import { MarkConvAckTuple, NotifyEventPair, SceneType, StickTopInfo } from '../../utils/types'; import V2NIMLocalConversationServiceImpl from '../../V2NIMLocalConversationServiceImpl'; export declare class LocalConversationCloudService { private core; private service; constructor(w14: NIM, service: V2NIMLocalConversationServiceImpl); markAck(v14: MarkConvAckTuple[]): Promise<void>;             stickTop(t14: string, u14: boolean): Promise<number>; processSyncMarkConversationAck(to: string, scene: SceneType, timestamp: number): Promise<NotifyEventPair | undefined>; processSyncStickTopBatch(o14: StickTopInfo[]): Promise<void>;               processSyncStickTop(n14: StickTopInfo): Promise<void>;            private markNormalConversationAck;           private markSuperTeamConversationAck;             private markMultiNormalConversationAck;             private markMultiSuperTeamConversationAck; private markConversationAck; private markMultiConversationAck; private addStickTop; private cancelStickTop;   private sceneToType; private typeToScene; } 